[defaults]
inventory           = inventory/hosts.ini
remote_user         = ubuntu
host_key_checking   = False
retry_files_enabled = False
roles_path          = roles

[privilege_escalation]
become        = True
become_method = sudo
become_user   = root

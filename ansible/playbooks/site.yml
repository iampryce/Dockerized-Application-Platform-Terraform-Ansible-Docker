---
- name: Configure Admin Server
  hosts: admin
  become: true

  roles:
    - common
    - users
    - security

- name: Configure Application Server
  hosts: application
  become: true

  roles:
    - common
    - users
    - security
    - docker
    - application
